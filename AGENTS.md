# Hollow native workstation
Read docs/DIRECTION.md, docs/ROADMAP.md and docs/CHECKPOINT.md first. Follow the newest user direction. Manage all files and tools; the user directs from Android and wants a fun downloadable game.

This is the canonical Godot project. Do not replace it with another browser-only simulation. Keep the prior Site at /workspace/sites/hollow intact unless a user request concerns it. Use Godot 4.5.1 GDScript, simulation separate from UI. Create original assets, keep dependency licenses, and do not introduce proprietary/copied game assets.

Preserve physical item ownership, transfer quantities/capacity, visibility, connected generation and stairs. Validate meaningful changes with the headless Godot tests, a scene launch, and available visual QA. Preserve valid tests and compatible saves. Keep plans short and prioritize implementation.

The owner designated https://github.com/xDoomKitty/Hollow.git as the canonical source repository. Preserve its existing commits and the original native project history. Fetch and reconcile remote changes before publishing; never force-push over newer work. After the full-history import succeeds, persist code, tests, docs and original asset generators through GitHub. The saved 0.68 Hollow_Source.zip remains a historical recovery fallback; do not maintain a second live source archive in Library once GitHub is synced.

Keep the private Hollow_Workstation.md recovery index and working Android, Windows and Linux downloads in their existing Library identities, replacing them with current version guards. Scratch may disappear: record the remote source revision and working build hashes in that index. Exclude downloaded toolchains, caches and private signing material from public source and game downloads. Retain the same test signing key only in its separately private continuity bundle.

Select and finish the next useful feature or fix autonomously. Do not spend money or submit a store release without the necessary owner authorization. Do not change unrelated automations or projects.
