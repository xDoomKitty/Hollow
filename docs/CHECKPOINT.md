# Hollow native checkpoint — 0.68.0

Canonical project: this Godot source tree. Saved owner direction: DIRECTION.md. Next work: ROADMAP.md. The earlier browser Site is preserved separately; do not overwrite it during native development.

Completed: the owner's requested touch improvements. Inventory panes, journal, build recipes and the colonist command list scroll by pressing and dragging their contents, including disabled rows. A short tap retains ordinary button behavior; drag release never issues a map order. An object tap starts a saved physical approach: containers are searched and opened on arrival, traveler/recruit/patient/stair actions use existing work orders, beds rest, spent alarms reset, and structures, blueprints and choice-bearing landmarks open their controls on arrival. Item transfers still require explicit quantities; arrival never chooses a story branch, dismantles a structure or cancels a blueprint. Original pawn identity, cancellation, queue semantics and older saves are retained. The campaign remains at Wayfarer Commons, depth 53. The staged Android, Windows and Linux builds are version 0.68.0.

Verified: 2,040 simulation assertions and 1,605 native headless input/layout assertions, zero failures. See VALIDATION.md for coverage and platform limits. Export, Android signing/manifest and Linux headless-launch results are recorded in VALIDATION.md. Graphical display startup was unavailable; adb reported no attached devices. No actual Android or Windows runtime test is claimed.

Next useful batch: a **mobile expedition overview** for locating essential cargo owners and regrouping colonists across persistent depths. Improve expedition usability before adding another campaign floor.

Persistence: the owner designated https://github.com/xDoomKitty/Hollow.git for this native project. Repository write access is verified by setup commit b8e554c32d560543ea60110ca505b97f15051192 and its readback. The owner authorized importing the existing source and full history. Preserve that remote commit while importing the native history; record completion in docs/GITHUB_IMPORT.md and the private workstation index. After import, GitHub is canonical for code, tests, docs, asset generators and git history. Fetch and reconcile it before editing or publishing; never force-push. The saved 0.68 Hollow_Source.zip and its full Hollow-history.bundle remain a recovery fallback. Keep current download/signing identities and the private workstation index, which must not be published as game content.

Delivery status: all six ordered 0.68 source/download/manifest/workstation replacements succeeded. The delivered game source is 7c3d1f25b40f61b30f46659fbb314f34adb19902. This repository migration changes documentation and persistence only; the signed Android code 68 and desktop downloads remain that validated release. The private workstation index records build hashes and remote import status.

Recovery: read the private current workstation index, fetch the GitHub repository and read AGENTS.md. Reconcile any newer local work. If GitHub recovery is unavailable, restore the historical source bundle and compare it with the latest recorded remote revision before editing. `tools/bootstrap.py` fetches the pinned Linux engine and selected templates; template download is large (~1.3 GB). `tools/fetch_android_tools.py` restores build-tools 35.0.1 and platform tools, using existing Java 17. In this workstation the engine is `tools/Godot_v4.5.1-stable_linux.x86_64`, Java `/usr/lib/jvm/java-17-openjdk-amd64`, and Android SDK `tools/android-sdk`. Configure Godot Editor Settings export/android paths; do not blindly overwrite other editor settings.

Android continuity: package com.hollow.colony.prototype; verified build code 68/name 0.68.0 follows delivered code 67/name 0.67.0. Increment versionCode for each delivered update. Restore the SAME test key from the private Hollow_Test_Signing.zip continuity artifact if missing. Current key path `/root/.local/share/godot/keystores/debug.keystore`, standard Godot debug alias `androiddebugkey` and development password `android`. These are test credentials, not a commercial release key. The private key bytes never belong in a source/game download or public repository. Keep its Library identity private and stable. Before public store release, plan production signing and migration deliberately.

Commands: `python3 tools/build.py --android`; verify with Android build-tools apksigner and aapt. `python3 tools/run_native_qa.py --visual` needs a real working display. `tools/package.py OUTPUT_DIRECTORY` packages current committed source/history and exports with notices. Do not include QA WebAssembly or toolchain files. Commit first; package source only from git-tracked paths. Never claim tests that the environment cannot perform.

Scheduled work is configured separately in the saved workstation index. Resume the existing task; do not create another four-hour automation. New user direction takes precedence. The assistant owns implementation and files; the user should only need to download, play and give direction.
