# Native 0.71 validation

September 21, 2026. Godot 4.5.1 stable on the hosted Linux workstation.

- Simulation: **80 groups, 2,148 assertions, zero failures**. New coverage verifies resident, concealed and public approaches; physical tally and survey-kit custody; exact lit/hidden costs; cancellation; active-work saves; warned threats; route benefits; connected topology; legacy migration and byte-stable round trips. Prior native saves, ownership conservation, connected travel and the full campaign remain covered.
- Native headless input/layout: **1,704 assertions, zero failures**: 1,579 campaign checks, 61 direct interaction/gesture checks and 64 expedition checks. Real viewport input verifies the Underway contextual panel, physical tally gating, survey-kit transfer, exact-choice work and saved outcome at desktop and phone landscape sizes. These are headless checks, not graphical or physical-device approval.
- Godot import, simulation, native QA and Android/Windows/Linux exports passed with the restored pinned SDK. Exported Linux starts headlessly with embedded resources.
- Android **0.71.0 / code 71** uses package `com.hollow.colony.prototype`, minimum API 24 and target API 35. APK Signature Schemes v2/v3 verify with the preserved certificate SHA-256 `df7193174d83fa04c2be605ebcfb4c989c302fc714705674b449d3b60cbdef62`. `tools/verify_android_signing.py` refuses an absent or mismatched key.
- The 0.71 code commit `3444f41616848fda3d7fd7234471dfac1ff22626` was prepared as a fast-forward from the reconciled 0.70 checkpoint. Package archive integrity, engine notices, private-key exclusion, signatures and packaged Linux launch passed before delivery.

Graphical QA remains unavailable because the workstation has no display server. `adb` reports no attached devices. Actual Android/Windows execution, rendering, sound, touch comfort, update installation, performance and battery behavior remain unverified.

No new raster art, audio or third-party dependency is required. Underway Fork uses original code-native map symbols and existing original audio. This is a prototype, not a store-ready release.
